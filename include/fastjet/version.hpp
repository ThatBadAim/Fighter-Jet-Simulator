#pragma once

namespace fastjet {

/// Application version shown in the menus. Keep in step with project(VERSION) in CMakeLists.txt.
inline constexpr const char* kVersionString = "1.0.0";

} // namespace fastjet
